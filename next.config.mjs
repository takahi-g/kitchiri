/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/kitchiri',
  assetPrefix: '/kitchiri/',
  images: { unoptimized: true },
};

export default nextConfig;
